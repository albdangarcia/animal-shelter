# ==================================
# === CORE APPLICATION SECRETS ===
# ==================================
# Generate with `openssl rand -base64 32`
AUTH_SECRET=""

# Password for the default admin user created during database seeding
ADMIN_PASSWORD=""

# Vercel Blob Storage token (required for image uploads)
BLOB_READ_WRITE_TOKEN=""

# (Optional) GitHub OAuth credentials for user login
AUTH_GITHUB_ID=""
AUTH_GITHUB_SECRET=""


# ==================================
# === DATABASE CONNECTION      ===
# ==================================
# Prisma uses the DATABASE_URL. For local development with Docker,
# we construct it from the variables below. For Vercel/production,
# you will replace this with the connection string from your provider.

DATABASE_URL="postgresql://postgres:mysecretpassword@postgres:5432/postgres"

# --- Local Docker PostgreSQL Configuration ---
# These variables are used to build the DATABASE_URL above for local development.
# You generally don't need to change these if using Docker Compose.
POSTGRES_USER="postgres"
POSTGRES_PASSWORD="mysecretpassword"
POSTGRES_HOST="postgres" # This is the service name in docker-compose.yml
POSTGRES_DB="postgres"
POSTGRES_PORT="5432"

# ==================================
# === Reporting                  ===
# ==================================
# IANA timezone the shelter operates in. All report date-range boundaries are
# computed in this zone so every viewer sees identical numbers. Defaults to
# "America/New_York" when unset.
SHELTER_TIMEZONE="America/New_York"

# ==================================
# === Demo Setup                 ===
# ==================================
# Flag for demo, it will show the demo banner and different 
# implementation for uploading the animal images
NEXT_PUBLIC_IS_DEMO=false
# Secret to reset the data for the demo
CRON_SECRET=""